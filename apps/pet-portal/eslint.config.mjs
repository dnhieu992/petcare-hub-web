import next from "@petcare/config/eslint/next";

// When wiring the app, prepend the Next.js plugin (ships with the app):
//   import nextPlugin from "@next/eslint-plugin-next";
//   export default [ ...nextPlugin.flatConfig.coreWebVitals, ...next ];
export default next;
